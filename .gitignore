# Segredos — NUNCA versionar (contém a chave service_role do Supabase)
.env
.env.local
.env.*.local

# Banco SQLite antigo (o app migrou para o Supabase) — contém dados de clientes
indycar.sqlite
*.sqlite
*.sqlite-*
indycar.sqlite*

# Dependências (não há, mas por segurança)
node_modules/

# Ferramentas locais / específicas da máquina
.claude/
iniciar-servidor.vbs

# Logs e lixo de SO
*.log
npm-debug.log*
.DS_Store
Thumbs.db
